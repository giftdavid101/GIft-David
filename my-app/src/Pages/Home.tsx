import React from "react";
import styled from "styled-components";

const HomeStyle = styled.div``;

const Home = () => {
  return (
    <HomeStyle>
      <div>{}</div>
    </HomeStyle>
  );
};

export default Home;
